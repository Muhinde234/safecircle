spring:
  config:
    activate:
      on-profile: prod

  # Recommended: set real values through environment variables in production.
  # datasource:
  #   url: ${DB_URL}
  #   username: ${DB_USERNAME}
  #   password: ${DB_PASSWORD}

server:
  port: ${PORT:8089}

logging:
  level:
    root: WARN
    org.springframework.web: INFO
    org.example.safecircle_backend: INFO
